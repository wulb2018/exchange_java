<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wulb2018.settlement.mapper.AccountMapper">
    <update id="frozenAsset" >
        UPDATE `t_account` SET available = available - #{frozen}, frozen = frozen + #{frozen},modify_date=#{modifyDate} WHERE user_id=#{userId}  AND asset = #{asset};
    </update>
</mapper>